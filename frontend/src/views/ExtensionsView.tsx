export function ExtensionsView() {
  return (
    <div className="view-shell">
      <div className="placeholder">
        <h1>Extensions <em>—</em> 04</h1>
        <p className="lede">
          Skills, plugins and MCP servers — what's installed, what's enabled,
          and where the manifest lives.
        </p>
        <p className="stub-note">step 6 of build sequence · backend /api/extensions is ready</p>
      </div>
    </div>
  );
}
